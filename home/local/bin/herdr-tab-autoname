#!/bin/sh
# Keep the extensionless command stable while Node uses the .ts suffix to
# enable its built-in TypeScript type stripping.
script_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd -P)
exec node "$script_dir/herdr-tab-autoname.ts" "$@"
