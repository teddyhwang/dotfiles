/**
 * Retired: shop-pi-fy's session-banner extension owns Pi session names and
 * their Herdr tab labels. Keep this no-op so existing dotfiles links remain
 * valid without loading a second, competing naming implementation.
 */
export default function sessionTabNameExtension(): void {}
